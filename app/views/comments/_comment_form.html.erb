<%= form_with model: [post, post.comments.build ] do |f| %>
  <%= f.hidden_field :user_id, value: current_user.id %>
  <%= f.text_area :body, placeholder: 'Share your thoughts' %>
  <%= f.submit 'Comment' %>
<% end %>